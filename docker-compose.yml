version: '3.9'

services:
  frotend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_BACKEND_API_URL
    env_file:
      - .env
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf