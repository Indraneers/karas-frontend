import { Button } from "@/components/ui/button";

export function RestockButton() {
  return (
    <Button className="border-2 border-primary text-primary" variant='outline'>
      Restock
    </Button>
  );
}