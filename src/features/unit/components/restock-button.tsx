import { Button } from "@/components/ui/button";

export function RestockButton() {
  return (
    <Button className="border-2 border-primary hover:bg-primary h-10 text-primary" variant='outline'>
      Restock
    </Button>
  );
}